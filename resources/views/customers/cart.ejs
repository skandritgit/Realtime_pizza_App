<section class="cart py-16">
<% if (session.cart) { %>
    

    <div class="order container mx-auto w-1/2">
        <div class="flex item-center border-b border-gray-300 pb-4">
        <img src="/img/cart-black.png" alt="">
        <h1 class="font-bold ml-4 text-2xl">Order Summary</h1>
        </div>
        <div class="pizza-list">
            <% for(let pizza of Object.values(session.cart.items)){ %>
            <div class="flex items-center my-8">
                <img class="w-24"  src="/img/<%= pizza.item.image %>" alt="">
                <div class="ml-4 flex-1">
                    <h1><%= pizza.item.name %></h1>
                    <span><%= pizza.item.size %></span>
                </div>
                <span class="flex-1"><%= pizza.qty %> Pcs</span>
                <span class="font-bold">₹<%= pizza.item.price * pizza.qty %></span>
            </div>
<% } %>
        </div>
        <hr>
       <div class="text-right py-4">
        <div>
            <span class="text-lg font-bold ">Total Amount:</span>
            <span class="account text-2xl font-bold ml-2">₹<%=session.cart.totalprice  %></span>
        </div>
        <div>
            <form action="" class="mt-12">
                <input class="border border-gray-500 w-1/2 p-2 mb-4" type="Number" placeholder="Phonenumber">
                <input class="border border-gray-500 w-1/2 p-2" type="text" placeholder="Enter your address ">
            <div>
                <button class="btn-primary py-2 px-6 rounded-full mt-6 font-bold text-white"  type="submit">Order now</button>
            </div>
            </form>
        </div>
       
        <a class="inline-block px-6 py-2 rounded-full btn-primary text-lg text-white mt-2 font-bold" href="">Login to continue</a>
    </div>
     </div> 
<%}else { %>




     <div class="empty-cart py-16">
    <div class="container mx-auto text-center">
        <h1 class="text-3xl font-bold mb-2">Cart Empty 😕</h1>
        <p class="text-black-500 text-lg mb-12">You havn't Ordered a pizza yet.<br>
            To Order a pizza , go to the main page.
        </p>
        <img src="img/empty-cart.png"  class="w-2/5 mx-auto" alt="empty-cart">
        <a href="/" class="inline-block px-6 py-2 rounded-full btn-primary font-bold mt-12">Go Back</a>

    </div>
</div> -->
<% } %>
</section>